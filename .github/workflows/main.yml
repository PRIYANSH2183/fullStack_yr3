name: My first workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print message
        run: |
          echo "Hello, GitHub Actions!"
          echo "Priyansh"